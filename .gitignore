# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# sqlite dev db
/prisma/dev.db
/prisma/dev.db-journal

# uploaded report cards (PII) — never commit
/storage/reportcards/*
!/storage/reportcards/.gitkeep

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Data snapshot taken during the Supabase migration. Contains real user
# rows (emails, password hashes) — never commit it.
/prisma/data-migration/snapshot.json
